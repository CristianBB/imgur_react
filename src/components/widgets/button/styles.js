import { StyleSheet } from 'react-native'
import * as colors from '../../../commons/colors'

export default StyleSheet.create({
    buttonContainer: { 
        backgroundColor: 'rgb(54,0,148)',
        padding: 20,
        paddingHorizontal: 40,
        alignItems: 'center',
        justifyContent: 'center', 
        borderRadius: 14,
        marginHorizontal: 20,
        height: 70,
    },
    buttonText: {
        fontWeight: '600',
        color: 'white'
    }
})